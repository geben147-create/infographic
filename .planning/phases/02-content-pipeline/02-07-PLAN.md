---
phase: 02-content-pipeline
plan: 07
type: execute
wave: 4
depends_on: ["02-05", "02-06"]
files_modified:
  - tests/test_pipeline_integration.py
  - tests/test_multi_channel.py
  - .env.example
autonomous: false
requirements: [PIPE-01, PIPE-02, PIPE-03, PIPE-04, PIPE-05, PIPE-06, VGEN-01, VGEN-02, VGEN-03, CHAN-01, CHAN-02]

must_haves:
  truths:
    - "Integration test validates full activity chain with mocked external services"
    - "Two channels with different configs produce valid pipeline outputs"
    - "Cost tracking writes correct entries for both fal.ai and Ken Burns paths"
    - "API endpoints return correct response shapes per UI-SPEC"
    - "Provider selection varies by channel config (channel_01 uses local, channel_02 uses fal)"
  artifacts:
    - path: "tests/test_pipeline_integration.py"
      provides: "End-to-end pipeline integration tests with mocked services"
      contains: "ContentPipelineWorkflow"
    - path: "tests/test_multi_channel.py"
      provides: "Multi-channel validation tests (CHAN-01, CHAN-02)"
      contains: "channel_01"
  key_links:
    - from: "tests/test_pipeline_integration.py"
      to: "src/workflows/content_pipeline.py"
      via: "test exercises workflow through mocked activities"
      pattern: "ContentPipelineWorkflow"
    - from: "tests/test_multi_channel.py"
      to: "src/models/channel_config.py"
      via: "test loads both channel configs and validates different provider selection"
      pattern: "load_channel_config"
---

<objective>
Validate the complete pipeline with integration tests and multi-channel verification.

Purpose: This is the final verification plan. It proves that all activities chain correctly, both channels work through the same workflow code, cost tracking is accurate, and API responses match the UI-SPEC contract.
Output: Integration test suite, multi-channel tests, updated .env.example.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-content-pipeline/02-RESEARCH.md
@.planning/phases/02-content-pipeline/02-UI-SPEC.md
@src/workflows/content_pipeline.py
@src/models/channel_config.py
@src/schemas/pipeline.py
@src/services/cost_tracker.py
@src/api/pipeline.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pipeline integration tests (mocked services)</name>
  <files>tests/test_pipeline_integration.py</files>
  <read_first>src/workflows/content_pipeline.py, src/activities/script_gen.py, src/activities/image_gen.py, src/activities/tts.py, src/activities/video_gen.py, src/activities/video_assembly.py, src/activities/thumbnail.py, src/activities/youtube_upload.py, src/schemas/pipeline.py, .planning/phases/02-content-pipeline/02-UI-SPEC.md</read_first>
  <behavior>
    - Test: PipelineParams serializes/deserializes correctly through Pydantic data converter
    - Test: All activity I/O models round-trip through JSON serialization
    - Test: Script generation activity produces valid Script JSON (mocked Ollama)
    - Test: Image gen activity saves PNG file (mocked ComfyUI)
    - Test: TTS activity saves WAV file (mocked CosyVoice)
    - Test: Video gen activity creates MP4 via Ken Burns when vgen_enabled=False (mocked ffmpeg)
    - Test: Video gen activity creates MP4 via fal.ai when vgen_enabled=True (mocked fal_client)
    - Test: Cost tracker log and retrieval works for a full pipeline run
    - Test: API trigger endpoint returns PipelineTriggerResponse shape
    - Test: API status endpoint returns PipelineStatusResponse shape
    - Test: API cost endpoint returns CostDetailResponse shape
  </behavior>
  <action>
    Create `tests/test_pipeline_integration.py`:
    
    **Section 1: Activity I/O serialization tests**
    - For each activity (script_gen, image_gen, tts, video_gen, video_assembly, thumbnail, youtube_upload):
      - Create input model instance with valid data
      - Serialize to JSON, deserialize back
      - Assert round-trip equality

    **Section 2: Activity chain tests (individual activities with mocked external deps)**
    - `test_script_gen_produces_valid_script`: Mock httpx POST to return valid Script JSON. Call generate_script with tmp_path run_dir. Assert script.json written with correct fields.
    - `test_image_gen_saves_png`: Mock ComfyUI WebSocket + HTTP. Call generate_scene_image. Assert scene_00.png exists.
    - `test_tts_saves_wav`: Mock TTS engine. Call generate_tts_audio. Assert scene_00.wav exists.
    - `test_video_gen_ken_burns`: Set vgen_enabled=False in channel config. Mock ffmpeg. Assert Ken Burns method used, cost=0.0.
    - `test_video_gen_fal_ai`: Set vgen_enabled=True, set FAL_KEY. Mock fal_client. Assert ai_video method, cost > 0.
    - `test_thumbnail_creates_jpeg`: Create test PNG in images dir. Call generate_thumbnail. Assert thumbnail.jpg exists and < 2MB.

    **Section 3: Cost tracking integration**
    - `test_cost_tracker_full_run`: Log 5 entries for a workflow. Assert get_run_total returns correct sum. Assert get_run_breakdown returns 5 entries.

    **Section 4: API response shape tests**
    - Use FastAPI TestClient
    - `test_trigger_response_shape`: POST /api/pipeline/trigger with mock Temporal client. Assert response matches PipelineTriggerResponse fields.
    - `test_status_response_shape`: GET /api/pipeline/status/{id} with mock workflow handle. Assert response matches PipelineStatusResponse fields including current_step.
    - `test_cost_response_shape`: GET /api/pipeline/cost/{id} with seeded cost_log. Assert response matches CostDetailResponse with breakdown array.

    Use conftest.py fixtures for: tmp pipeline dirs, mock channel configs (write channel_01.yaml and channel_02.yaml to tmp_path), mock .env settings.
  </action>
  <verify>
    <automated>cd C:/WINDOWS/system32 && uv run pytest tests/test_pipeline_integration.py -v --tb=short</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_pipeline_integration.py contains tests for all 7 activities (script_gen, image_gen, tts, video_gen, video_assembly, thumbnail, youtube_upload)
    - tests/test_pipeline_integration.py contains `test_cost_tracker_full_run`
    - tests/test_pipeline_integration.py contains `test_trigger_response_shape` and `test_status_response_shape` and `test_cost_response_shape`
    - tests/test_pipeline_integration.py contains `TestClient` from FastAPI
    - tests/test_pipeline_integration.py exits 0
  </acceptance_criteria>
  <done>Integration tests validate full pipeline activity chain, cost tracking, and API response shapes with mocked external services.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Multi-channel validation tests</name>
  <files>tests/test_multi_channel.py, .env.example</files>
  <read_first>src/models/channel_config.py, src/channel_configs/channel_01.yaml, src/channel_configs/channel_02.yaml, src/models/provider.py, src/services/provider_registry.py</read_first>
  <behavior>
    - Test: channel_01.yaml loads as ChannelConfig with local providers (local:cosyvoice2, local:sdxl-juggernaut, local:qwen3.5-9b)
    - Test: channel_02.yaml loads as ChannelConfig with mixed providers (local:kokoro, fal:flux-kontext, together:qwen3-8b, fal:kling-2.5-turbo)
    - Test: ModelSpec.parse correctly resolves all model specs from both channels
    - Test: channel_01 has vgen_enabled=False, channel_02 has vgen_enabled=True
    - Test: Both channels use different prompt_template values
    - Test: Both channels have different tags lists
    - Test: ChannelConfig is frozen — mutation raises error
    - Test: ContentPipelineWorkflow accepts both channel_ids without error (mock all activities)
  </behavior>
  <action>
    Create `tests/test_multi_channel.py`:

    **Section 1: Config loading**
    - `test_channel_01_loads_with_local_providers`: Load channel_01.yaml, assert tts_model=="local:cosyvoice2", image_model=="local:sdxl-juggernaut", llm_model=="local:qwen3.5-9b", vgen_enabled==False
    - `test_channel_02_loads_with_mixed_providers`: Load channel_02.yaml, assert tts_model=="local:kokoro", image_model=="fal:flux-kontext", video_model=="fal:kling-2.5-turbo", vgen_enabled==True
    - `test_channel_configs_have_different_templates`: Assert channel_01.prompt_template != channel_02.prompt_template
    - `test_channel_configs_have_different_tags`: Assert channel_01.tags != channel_02.tags

    **Section 2: Provider resolution**
    - `test_model_spec_parse_all_channel_01_models`: Parse each model spec from channel_01, assert correct ProviderType
    - `test_model_spec_parse_all_channel_02_models`: Parse each model spec from channel_02, assert correct ProviderType

    **Section 3: Frozen model**
    - `test_channel_config_immutable`: Load config, attempt `config.niche = "tech"`, assert TypeError raised

    **Section 4: Workflow compatibility**
    - `test_workflow_params_accept_both_channels`: Create PipelineParams with channel_id="channel_01" and "channel_02", assert both serialize correctly

    Update `.env.example` with all Phase 2 env vars and documentation comments.
  </action>
  <verify>
    <automated>cd C:/WINDOWS/system32 && uv run pytest tests/test_multi_channel.py -v --tb=short</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_multi_channel.py contains `test_channel_01_loads_with_local_providers` and `test_channel_02_loads_with_mixed_providers`
    - tests/test_multi_channel.py contains `test_channel_config_immutable`
    - tests/test_multi_channel.py contains `test_model_spec_parse_all_channel`
    - tests/test_multi_channel.py contains assertions for `local:cosyvoice2` and `fal:kling-2.5-turbo`
    - tests/test_multi_channel.py exits 0
  </acceptance_criteria>
  <done>Both channels validated through same workflow code. Provider selection differs correctly per channel config. Frozen model prevents mutation.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verification of complete Phase 2 pipeline</name>
  <files>tests/</files>
  <action>Human verifies full test suite, API docs, and channel configs after all automated work is complete.</action>
  <verify>Human confirms all checks pass</verify>
  <done>Human approves Phase 2 completion</done>
  <what-built>Complete content pipeline: topic input -> script gen -> image gen -> TTS -> video gen/Ken Burns -> FFmpeg assembly -> thumbnail -> YouTube upload, with provider-swappable architecture and multi-channel support</what-built>
  <how-to-verify>
    1. Run full test suite: `cd C:/WINDOWS/system32 && uv run pytest tests/ -v --cov=src --cov-report=term-missing`
    2. Verify all tests pass (0 failures)
    3. Check coverage report — all new Phase 2 files should have test coverage
    4. Verify imports work: `uv run python -c "from src.workflows.content_pipeline import ContentPipelineWorkflow; from src.api.pipeline import router; print('OK')"`
    5. Verify API starts: `uv run uvicorn src.main:app --host 0.0.0.0 --port 8000` then visit http://localhost:8000/docs — check /api/pipeline/* endpoints appear
    6. Check channel configs: `uv run python -c "from src.models.channel_config import load_channel_config; c1=load_channel_config('channel_01'); c2=load_channel_config('channel_02'); print(c1.tts_model, c2.tts_model)"`
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
- `uv run pytest tests/ -v --cov=src --cov-report=term-missing` — full suite passes
- All Phase 2 requirement IDs covered by at least one test
- API endpoints match UI-SPEC response schemas
- Both channels work through same workflow code
</verification>

<success_criteria>
All Phase 2 requirements verified through integration and multi-channel tests. Pipeline callable end-to-end (with mocked external services). Human verifies test suite and API documentation.
</success_criteria>

<output>
After completion, create `.planning/phases/02-content-pipeline/02-07-SUMMARY.md`
</output>
